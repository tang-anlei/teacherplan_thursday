<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All coach assignments
    </h1>

    <a href="/coach_assignments/new" class="btn btn-block btn-outline-secondary">
      Add a new coach assignment
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          User
        </th>

        <th>
          Improvement
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @coach_assignments.each do |coach_assignment| %>
      <tr>
        <!-- Display coach_assignment.id start -->
        <td>
          <%= coach_assignment.id %>
        </td>
        <!-- Display coach_assignment.id end -->

        <!-- Display coach_assignment.user_id start -->
        <td>
          <% if coach_assignment.user.present? %>
            <a href="/users/<%= coach_assignment.user.id %>">
              <%= coach_assignment.user.email %>
            </a>
          <% end %>
        </td>
        <!-- Display coach_assignment.user_id end -->

        <!-- Display coach_assignment.improvement_id start -->
        <td>
          <% if coach_assignment.improvement.present? %>
            <a href="/improvement_plans/<%= coach_assignment.improvement.id %>">
              <%= coach_assignment.improvement.state %>
            </a>
          <% end %>
        </td>
        <!-- Display coach_assignment.improvement_id end -->

        <!-- Display coach_assignment.created_at start -->
        <td>
          <%= time_ago_in_words(coach_assignment.created_at) %> ago
        </td>
        <!-- Display coach_assignment.created_at end -->

        <!-- Display coach_assignment.updated_at start -->
        <td>
          <%= time_ago_in_words(coach_assignment.updated_at) %> ago
        </td>
        <!-- Display coach_assignment.updated_at end -->

        <td>
          <a href="/coach_assignments/<%= coach_assignment.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @coach_assignments, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
