<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All goals
    </h1>

    <a href="/goals/new" class="btn btn-block btn-outline-secondary">
      Add a new goal
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "goal_templates/goals_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Improvement plan
        </th>

        <th>
          Comments
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @goals.each do |goal| %>
      <tr>
        <!-- Display goal.id start -->
        <td>
          <%= goal.id %>
        </td>
        <!-- Display goal.id end -->

        <!-- Display goal.improvement_plan_id start -->
        <td>
          <% if goal.improvement_plan.present? %>
            <a href="/improvement_plans/<%= goal.improvement_plan.id %>">
              <%= goal.improvement_plan.state %>
            </a>
          <% end %>
        </td>
        <!-- Display goal.improvement_plan_id end -->

        <!-- Display goal.comments start -->
        <td>
          <%= goal.comments %>
        </td>
        <!-- Display goal.comments end -->

        <!-- Display goal.created_at start -->
        <td>
          <%= time_ago_in_words(goal.created_at) %> ago
        </td>
        <!-- Display goal.created_at end -->

        <!-- Display goal.updated_at start -->
        <td>
          <%= time_ago_in_words(goal.updated_at) %> ago
        </td>
        <!-- Display goal.updated_at end -->

        <td>
          <a href="/goals/<%= goal.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @goals, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
